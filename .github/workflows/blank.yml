name: First_workflow
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      -name:print greeting
       run: echo "hello"
      -name:print goodbye
       run: echo :"done-bye"
